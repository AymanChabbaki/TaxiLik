# Expo SDK

This project targets **Expo SDK 54** (downgraded from 56 so the public Expo Go app
can run it on physical devices). Read the versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing native/Expo code.
